// import App from '../App';
// import ProductOverview from '../components/overview/ProductOverview';

describe('truth', () => {
  it('should be true', () => {
    expect(true).toBe(true);
  });
});

describe('Add to Cart', () => {
  it('should have two selectors', () => {});

  it('should not render options with no remaining stock', () => {});
  it('should have have a size selector which reads `OUT OF STOCK` for items with no remaining stock', () => {});
  it('should have a size selector which displays `SELECT SIZE` by default', () => {});
  it('should have a quantity selector which ranges from a minimum of 1 to a maximum of 15', () => {});
  it('should have a quantity selector which defaults to 1', () => {});
  it('should have an `Add to Cart` button', () => {});
  it('should hide the button if there is no stock', () => {});
  it('should prompt the user to select size if the `Add to Cart` button is clicked and the default size is selected', () => {});
});
